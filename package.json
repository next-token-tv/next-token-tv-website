{
  "name": "srt-halfwidth-review",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1 --directory ..",
    "build": "mkdir -p dist && cp index.html dist/index.html"
  }
}
